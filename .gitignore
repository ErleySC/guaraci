# --- Personal / local config & runtime state (kept out of the repo) ---
config.yaml
visual_config.json
codigos_usuario.json
codigos_exportados.csv
tecnica_usuario.json
.cli_wizard_done
.cli_modo_usuario
perfis/

# --- Results (large PNGs / joblib models — keep out of repo) ---
resultados_tcc/
resultados*/
*.joblib

# --- guaraci demo / guaraci doctor output (P7) ---
GUARACI_Demo/
guaraci_doctor.txt

# --- Local run drivers / logs (scratch, personal paths — keep out of repo) ---
# (vivem em scripts/, mas os padroes abaixo (sem barra) casam em qualquer nivel)
run_benchmark_tcc.py
gerar_relatorio_abnt.py
benchmark_run.log
*.log

# --- Saida gerada por scripts/benchmark_tecator.py (reproduzivel, nao versionado) ---
resultados_tecator.json

# --- Research data (kept local; use modo=sintetico to test without real data) ---
dados/
*.dx
dados_oleos/

# --- Cache local de datasets PUBLICOS de terceiro (Corn, Mendeley) --
# NUNCA versionar o dado em si (licenca de terceiro -- ver
# docs/VALIDACAO_PUBLICA.md e datasets/README.md). Baixado sob demanda por
# scripts/download_datasets/*.py ou pelo job validacao-publica* do CI;
# aponte GUARACI_DATASETS_DIR para a pasta que os contem.
datasets_publicos/
*.mat

# --- Local archive of old versions / scratch (kept out of the repo) ---
_test*/
"pineline_quimiometria_14 copy.py"
*\ copy.py

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# --- Artefatos de build de pacote (sdist/wheel; regeraveis por `uv build`) ---
build/
dist/

# --- Test / coverage artifacts ---
.coverage
coverage.xml
htmlcov/
.ruff_cache/
.pytest_cache/
# Banco de dados de exemplos falhos do Hypothesis: cache local, nao fonte
# de verdade -- casos que importam ficam versionados como @example no
# codigo (Bloco 13d, Frente 2), nao aqui.
.hypothesis/

# --- MkDocs build output (gerado pelo CI, nao versionado) ---
site/
docs/SECURITY.md

# --- Virtual environments ---
.venv/
venv/
env/

# --- Editors / OS ---
.vscode/
.idea/
.DS_Store
Thumbs.db

# --- Streamlit ---
.streamlit/secrets.toml

# --- Claude Code project memory (local only) ---
.claude/

# --- Code review graph (local tool artifact) ---
.code-review-graph

# --- Saidas de execucao/diagnostico locais (contem caminhos pessoais) ---
sanity_out.txt
val_out.txt
*_out.txt

# --- Artefatos de sessao de ferramenta (llm-council, auditorias de sessao) ---
# Sao registros de UMA sessao, nao documentacao do projeto: envelhecem mal
# (citam numeros de estado que mudam a cada commit) e poluem o git status.
council-*
docs/_*.md

# Notas de trabalho e material de auditoria: local, nao versionado.
docs/auditoria/
CLAUDE.md
